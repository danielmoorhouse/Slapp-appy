@model SlappMain.Models.App

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>
<hr />
<h3>Are you sure you want to delete    @Html.DisplayFor(model => model.AppName)?</h3>
<hr />
<div class="container-fluid" style="text-align:center; height: 20em;">



    @if (Model.AppTile != null)
    {
        <img src="@Url.Content(Model.AppTile)" width="150" />
    }

    <h4> @Html.DisplayFor(model => model.AppName)</h4>




    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default revs" /> |

        </div>
    }
</div>
<hr/>
<div>
    <button class="btn btn-default revs">@Html.ActionLink("Back to List", "Index")</button>
</div>
<hr/>