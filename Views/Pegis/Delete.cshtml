@model SlappMain.Models.Pegi

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>
<hr/>
<h3>Are you sure you want to delete -&nbsp;<img src="@Url.Content(Model.PegImg)" width="50"/></h3>
<hr/>
<div class="container-fluid" style="text-align: center; height: 20em;">
  

      
        
        
            @if (Model.PegImg != null)
        {
            <img src="@Url.Content(Model.PegImg)" width="100" style="margin-top: 5%;" />
    }
        
    

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default revs" /> 
           
        </div>
    }
</div>
<hr/>
<div>
    <button class="btn btn-default revs">  @Html.ActionLink("Back to List", "Index")</button>
</div>
<hr/>
